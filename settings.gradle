include  \
    ':BetterKnife',
    ':sample',
    ':wear'

rootProject.name = 'BetterKnife'

def setBuildFile(project) {
  project.buildFileName = "${project.name.toLowerCase()}.gradle"
  project.children.each {
    setBuildFile(it)
  }
}

rootProject.children.each {
  setBuildFile(it)
}
